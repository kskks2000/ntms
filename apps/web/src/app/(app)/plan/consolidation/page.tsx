'use client';

import { ConsolidationView } from '@/components/plan/consolidation-view';

/** 편성 · 상차조합 — 오더를 골라 한 대에 묶는다 */
export default function ConsolidationPage() {
  return <ConsolidationView />;
}
