{
  "name": "ntms",
  "version": "0.1.0",
  "private": true,
  "description": "통합 연계 운송관리시스템 (Transport Management System)",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rimraf node_modules",
    "db:create": "psql -U postgres -h localhost -c \"CREATE DATABASE ntms ENCODING 'UTF8' TEMPLATE template0\"",
    "db:ddl": "psql -U postgres -h localhost -d ntms -v ON_ERROR_STOP=1 -f db/run_all.sql",
    "db:drop": "psql -U postgres -h localhost -c \"DROP DATABASE IF EXISTS ntms\"",
    "db:reset": "pnpm db:drop && pnpm db:create && pnpm db:ddl && pnpm db:pull",
    "_comment_prisma": "Prisma 는 .env 를 CWD 기준으로 찾는다. 저장소 루트에서 --schema 로 실행해야 루트 .env 가 로드된다.",
    "db:pull": "prisma db pull --schema packages/db/prisma/schema.prisma && pnpm db:generate",
    "db:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "db:studio": "prisma studio --schema packages/db/prisma/schema.prisma"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.4.0",
    "prisma": "^6.2.0",
    "rimraf": "^6.0.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "//": "pnpm 10 부터 postinstall 스크립트는 기본 차단된다(공급망 공격 방어). 네이티브 바이너리나 코드 생성이 필요한 패키지만 여기에 명시적으로 허용한다. 새 패키지를 추가할 때 install 경고에 뜨는 이름을 검토 후 넣을 것.",
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  }
}
